# Core dependencies
pandas>=2.0.0
pyarrow>=10.0.0
fastparquet>=0.8.0
psutil>=5.9.0

# Engine-specific dependencies
polars>=0.20.0
duckdb>=0.9.0
pyspark>=3.4.0
getdaft>=0.2.0
datafusion>=0.6.0

# Optional: for dbt and sqlmesh (install separately)
dbt-core>=1.10.0
dbt-duckdb>=1.9.0
# sqlmesh>=0.25.0